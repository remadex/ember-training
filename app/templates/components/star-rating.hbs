{{#each stars as |star|}} {{log star}}
<span class="rating-star" onclick={{action "setRating" star.rating}}>
    {{#if star.isFull}} {{fa-icon "star "}} {{else}} {{fa-icon "star-o "}} {{/if}}
</span>
{{/each}}